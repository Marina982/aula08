import styles from '../styles/Footer.module.css';

export default function Footer() {
  return (
    <header className={styles.footer}>
      <p className={styles.title}>Desenvolvedor: Marina Goes e Olavo Goes</p>
      <img
      />
    </header>
  );
}
