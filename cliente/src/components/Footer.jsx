import styles from '../styles/Footer.module.css';

export default function Footer() {
  return (
    <header className={styles.footer}>
      <p className={styles.title}>Desenvolvedor: Marina /**/ e Olavo /**/</p>
      <img
      />
    </header>
  );
}
